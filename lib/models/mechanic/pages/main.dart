import 'package:flutter/material.dart';

class MechanicScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Mechanic Screen'),
      ),
      body: Center(
        child: Text('This is the Mechanic Screen'),
      ),
    );
  }
}
